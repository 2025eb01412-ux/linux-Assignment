The lsof command displayed open files currently used by the system.

The ulimit command showed the system resource limits.

The ls command successfully redirected its output to output.txt.

The error generated by an invalid directory was redirected to error.txt.

This demonstrates how Linux separates standard output and standard error using file descriptors.
